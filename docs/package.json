{
  "name": "@digitalbranch/scaffold-docs",
  "version": "1.0.0",
  "description": "docs",
  "source": "src/index.html",
  "private": true,
  "scripts": {
    "start": "parcel -p 4321",
    "build": "parcel build --no-scope-hoist --no-optimize",
    "watch": "parcel watch",
    "clean:parcel-cache": "rm -rf ./.parcel-cache",
    "clean:dist": "rm -rf ./dist",
    "clean": "pnpm clean:parcel-cache ; pnpm clean:dist",
    "deploy:firebase": "pnpm clean && pnpm build && pnpm firebase deploy"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@digitalbranch/app": "file:.."
  },
  "devDependencies": {
    "firebase-tools": "^14.16.0",
    "parcel": "^2.15.4",
    "process": "^0.11.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
